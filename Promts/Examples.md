Add a file 'Examples.lean' where you import 'CategoricalLogic.Profunctor' you add the following
* The hom profunctor (for a pair of objects c and d it is simply the hom set Hom(c,d)). Don't forget to add a useful API
* Given two types C and D and a map C × D -> Type*, define a discrete profunctor from the discrete category on C to the discrete category on D.

Make sure to use 'Category*' instead of 'Category' wherever possible to make sure that the universe levels are as correct.

92766082-1052-44f4-b0d5-c90cf5c5496d